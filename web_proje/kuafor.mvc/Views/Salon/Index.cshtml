@model IEnumerable<kuafor.mvc.Models.Salon>

<h2>Salon Listesi</h2>
<table class="table">
    <thead>
        <tr>
            <th>Ad</th>
            <th>Adres</th>
            <th>Telefon</th>
            <th>İşlemler</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var salon in Model)
        {
            <tr>
                <td>@salon.Name</td>
                <td>@salon.Address</td>
                <td>@salon.PhoneNumber</td>
                <td>
                    <a asp-action="Details" asp-route-id="@salon.Id" class="btn btn-info">Detay</a>
                    <a asp-action="Edit" asp-route-id="@salon.Id" class="btn btn-warning">Düzenle</a>
                    <a asp-action="Delete" asp-route-id="@salon.Id" class="btn btn-danger">Sil</a>
                </td>
            </tr>
        }
    </tbody>
</table>
<a asp-action="Create" class="btn btn-primary">Yeni Salon Ekle</a>
